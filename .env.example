# Copy this file to .env and fill in values when Supabase is introduced in a future task.
# These are public, client-side variables (Expo bundles EXPO_PUBLIC_* into the app).
# Never put a service-role key here.

EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
